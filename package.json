{
  "name": "@formatho/runtime",
  "version": "0.2.0",
  "description": "Formatho Runtime \u2014 private, self-hosted MCP tool infrastructure for AI agents.",
  "type": "module",
  "bin": {
    "formatho-runtime": "dist/index.js"
  },
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "test": "node --test dist/*.test.js dist/tools/*.test.js 2>/dev/null || node --test dist/"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "zod": "^3.24.0",
    "@noble/hashes": "^1.8.0"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "@types/node": "^22.0.0"
  }
}